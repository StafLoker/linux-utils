alias ll='ls -lah'
alias llg='ls -lah | grep -i'

alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -i'

alias h='history'
alias hg='history | grep'

alias cmx='chmod +x'

alias f='find . -iname'

alias td='tree -d'
alias tdl='tree -d -L 4'

alias s='systemctl'
alias srt='systemctl start'
alias sp='systemctl stop'
alias sst='systemctl status'
alias sr='systemctl restart'
alias sdr='systemctl daemon-reload'

alias j='journalctl -xeu'
alias jctl='journalctl -u'
alias jrt='journalctl -fu'

alias d='docker'
alias dc='docker compose'
